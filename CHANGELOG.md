# EarCrate Changelog

## Unreleased — deterministic multi-island jam planning

- Add `EarcrateCore.propose_island_set`, which binds an explicit content-addressed island schedule to exact BPM/key decks and exact source allowlists.
- Preserve one global source and turnover ledger across every island; duplicate sources, stale pool identities, role-incomplete decks, insufficient phrase-aligned capacity, and weakened transform or turnover policies refuse before rendering.
- Lower every island through the existing TasteSpec composer with exact deck selection, then join the globally governed segments through the existing equal-power fade primitive.
- Emit full role stems plus an explicit transition/mastering residual so the delivered stems numerically reconcile to the master.
- Leave ordinary single-deck proposal and render paths untouched unless the new island-set entrypoint is invoked.

