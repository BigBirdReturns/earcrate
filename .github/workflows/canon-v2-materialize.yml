name: canon-v2-materialize

on:
  pull_request:
    branches: [main]
    paths:
      - ".github/canon_v2_materialize.py"
      - ".github/workflows/canon-v2-materialize.yml"

permissions:
  contents: read

jobs:
  materialize:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Transport retired; terminal files are written directly and verified by repository gates."
