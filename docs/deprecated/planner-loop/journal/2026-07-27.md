# 2026-07-27

## Children v1 — assemble the organs around one content-addressed specimen

**State:** The cephalopod reader, exact MIDI/player-piano authority, Gate 8 custody and continuation, sealed racks, live planner, and MixScore source transports existed across the active stack. The supplied `Children` proof established exact score extraction, a 69-to-105-measure performed traversal, 1,257 notes, independent treble/bass realization, and 19/19 source-transport operations. There was no canonical object proving which organs had passed on the same specimen, which branches were independent, or which whole-organism obligations remained blocked.

**Asked planner:** Continue from the independently audited proof pack. Treat the complete hereditary stack as the object, preserve source-media custody, refuse score contamination of audio inference, promote printed form and harmony into canonical musical objects, and report organ success separately from thesis success.

**Planner said:** Build a cross-organ specimen layer rather than another parser or renderer. Keep score and audio branches isolated until both seal; preserve `FormGraph` and `PerformancePath`; use existing `MusicEvent` and `MusicHarmonyFrame`; coordinate exact MIDI and MixScore evidence; make every absent organ an explicit blocker; and use `Children` as the first ground-truth fixture.

**Did:**
- Added a content-addressed specimen manifest and branch-lineage contract.
- Added score/audio ObservationLedgers, form graph, performed traversal, score answer key, convergence report, and Buffalo Gate receipt schemas.
- Added the `children_v1` score adapter, exact MIDI reconciliation, printed harmony canonicalization, and MixScore execution verification.
- Added an external bindings CLI and standalone-package embedding without committing the score PDF, recording, rendered audio, or private-library material.
- Integrated the MixScore source-transport organ onto the cephalopod/Gate 8 stack.
- Registered the reader, MixScore, and cross-organ specimen gate in the executable buffalo-harvest map.
- Added synthetic independence, determinism, schema, package-dispatch, and standalone gates.

**Verified:** The supplied external proof compiles to 1,257 canonical music events, 53 contiguous performed harmony frames, a 69-node form graph, and a 105-measure performance path. Exact MIDI semantic identity and 19/19 MixScore execution are retained. The current Buffalo Gate honestly reports six required organs passed and six blocked; it does not claim the whole organism has passed without independent recording inference, convergence, continuation, rack realization, review circulation, and evolutionary evidence.

**Follow-up:** Bind an owned or licensed recording, run the cephalopod branch without score access, freeze the convergence report, then execute and review one legal continuation plus an illegal negative control through MIDI, racks, and MixScore.

## Children v1 — integrated bootstrap and standalone authority

**State:** The source-free specimen payload was stacked on the cephalopod branch with the exact MixScore blobs as a second merge parent. The first hosted execution exposed two pre-existing single-file composition defects rather than weakening the new tests: the import-only music director facade lost its implementation aliases when flattened, and embedded package modules inherited `__name__ == "__main__"`, causing the project CLI to execute before top-level dispatch.

**Did:**
- Flattened `director_validation.py` and `director_render.py` instead of the import-only director facade.
- Added package-shaped import views over the already-loaded flattened MIDI, music, rack, reader, live, and MixScore authorities so retained cross-organ imports execute without a second implementation.
- Filtered dunder names from project and specimen bootstrap seeds, preserving embedded module identity and preventing accidental `__main__` execution.
- Kept the standalone Buffalo and MixScore tests intact.
- Applied the SHA-256-pinned payload, refused repository source media, removed the bootstrap chunks and workflow, and committed the ordinary reviewable tree.

**Verified:** Hosted bootstrap run `30337430879` completed every step: payload custody, 16 focused cross-organ/MixScore tests, generated single-file Buffalo and MixScore commands, OSS audit, diff check, source-media refusal, and final commit. The resulting branch head is `7f1aee8adf69f64aa28b09b446c3d401eae06e76`. A connector-authored journal commit follows solely to trigger the complete repository workflows, because GitHub intentionally suppresses normal recursive workflow execution from a commit pushed by `GITHUB_TOKEN`.

**Follow-up:** Require the complete repository gate and package-verifier workflows to pass on the ordinary branch head before marking PR #43 ready.
