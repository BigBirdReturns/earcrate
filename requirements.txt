numpy>=1.26,<3
scipy>=1.10
librosa>=0.10,<0.12
soundfile>=0.12
pyloudnorm>=0.1.1
mutagen>=1.47
# OPTIONAL — stem separation (v3 §5.2 DemucsStemProvider). The base install stays
# CPU-safe and does not pull a platform-specific Torch wheel. On a CUDA workstation,
# run scripts\RUN_STEM_PROVIDER_REPAIR.cmd. That governed path selects a supported
# official PyTorch CUDA wheel, installs pinned Torch/Demucs packages into EarCrate's
# live interpreter, performs a real current-source GPU separation, verifies the
# instrumental stem and source reconciliation, and only then writes
# stem_provider=demucs into the workspace config. A package install by itself is
# not provider authority.
