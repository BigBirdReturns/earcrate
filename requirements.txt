numpy>=1.26,<3
scipy>=1.10
librosa>=0.10,<0.12
soundfile>=0.12
pyloudnorm>=0.1.1
mutagen>=1.47
# OPTIONAL — stem separation (v3 §5.2 DemucsStemProvider). NOT installed by
# default: the shipped box runs the NoopStemProvider and never separates. To opt
# a real CUDA GPU box in, install torch (CUDA build for your platform) and demucs
# out of band, then set stem_provider=demucs (or env EARCRATE_STEMS=demucs):
#     pip install torch --index-url https://download.pytorch.org/whl/cu121
#     pip install demucs
# Left OUT of this file on purpose — torch is a large, platform/CUDA-specific
# dependency and the feature stays UNVERIFIED until a real GPU receipt exists.
#
# OPTIONAL — high-fidelity time-stretch / pitch-shift (TransformProvider seam,
# earcrate/providers/transform.py). NOT installed by default: the shipped box
# uses the phase vocoder (librosa) and every render is unchanged. To opt in,
# install the Rubber Band CLI binary + pyrubberband, then set
# EARCRATE_TRANSFORM=rubberband:
#     # the 'rubberband' binary must be on PATH (e.g. apt install rubberband-cli,
#     # brew install rubberband, or the Windows Rubber Band CLI build)
#     pip install pyrubberband
# Left OUT on purpose — it needs a native binary and the default-flip (with the
# ENGINE_VERSION bump) waits on a real ears verdict on the box. Check readiness
# with `earcrate doctor` (transform_capability).
