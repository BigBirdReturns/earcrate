# 2026-07-27

## 18:30 — MixScore source transports: build the turntables, not another crate
**State:** EarCrate already had a receding-horizon live planner, typed DJ technique
names, compiled sample racks, phrase preparation, and a fixed SPSC audio callback.
The committed live phrase nevertheless lowered through MIDI note/rack execution. It
could select and retrigger material but did not preserve independent source playheads,
so simultaneous decks, cue jumps, bounded source loops, hard cuts, and fades were not
one exact execution language.

**Asked planner:** Build the smallest end-to-end implementation that can prove the
missing medium now, reuse every trustworthy existing commodity and EarCrate invariant,
and sweep the active DJ/audio OSS community before choosing new dependencies. The
proof must be testable without private music, retain deterministic receipts and source
identity, avoid callback work, and refuse incomplete execution rather than falling
back.

**Planner said:** Keep the existing planner/crate/callback machinery, but add a sibling
`MixScore` source-transport runtime rather than extending the MIDI lowering until it
resembles a deck. Separate master beat, source beat, and source frame; expose deck
verbs; render N simultaneous channels; reconcile stems to the master; and use a
synthetic two-source performance as the first proof. Reuse ffmpeg, NumPy, SciPy,
SoundFile, source hashes, guarded writes, and the current callback boundary. Evaluate
Signalsmith Stretch and libsamplerate next; study GPL DJ systems without silently
linking them into the core.

**Did:**
- Added versioned MixScore, execution-ledger, and render-receipt schemas.
- Added an offline deterministic N-deck renderer with load/play/stop/cut, jump/seek,
  loop/exit-loop, rate and nudge, gain/fade/mute/pan, and equal-power crossfader
  operations.
- Added pre/post-decode file identity verification, bound sealed scores, exact event
  accounting, per-deck stems, master/stem reconciliation, deterministic PCM hashes,
  and atomic artifact replacement with the receipt committed last.
- Reused the existing anti-aliased `analyze.decode.resample_or_fit` primitive instead
  of maintaining a second resampling implementation.
- Added collision preflight so a render cannot overwrite a source, its input score, or
  another output artifact through a sanitized stem-name collision.
- Added `earcrate mix capability|demo|render|scaffold`, package dispatch, and the
  single-file build path.
- Added a self-contained two-source synthetic demo and executable gates covering all
  declared operations, N=3 routing, determinism, source mutation refusal, schemas,
  output-collision refusal, CLI output, and the packaged single-file command.
- Wrote `docs/MIXSCORE_RUNTIME.md` and `docs/DJ_ENGINE_OSS_SWEEP.md` with the retained
  authority boundary and provider/license decisions.

**Verified:** In the isolated implementation tree, the six module/CLI/schema/safety
gates that do not require the rest of the repository pass (`6 passed, 1 deselected`).
The 12-second demo renders two independent stems and a master, executes 15/15 events,
reconciles stems at zero numerical error, repeats deterministically, and refuses after
a source file mutation. All three emitted artifact classes validate against their
JSON Schemas. The first hosted run failed before executing gates because the new test
module imported `pytest`, which the repository's intentionally pytest-free custom gate
runner does not install; that test-only dependency was removed. The single-file gate
remains in the repository suite for the corrected hosted run.

**Follow-up:** Lower one committed live-planner phrase into a bounded MixScore window,
then prepare N deck blocks off-callback and feed the existing SPSC host. Benchmark
Signalsmith Stretch behind a receipt-bearing transform provider before claiming key
lock; add DJ EQ/filter automation only after transport semantics stay exact.
