"""A1-07 full-form descent: the first Album One lane long enough to audition.

Prior A1-07 work produced 18.95 s cores and a 38.15 s arc. Neither reaches the
45 s full-form floor, so neither can show whether the groove survives setup,
development and payoff. This package extends the qualified gold-v7 arc with an
explicitly mapped body and re-times only the donor band, under a bounded timing
law, so the owner hears a real form instead of a fragment.
"""

from .contract import FullFormError, load_contract

__all__ = ["FullFormError", "load_contract"]
